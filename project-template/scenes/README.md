# Proof scene template

Copy `ProofScene.tsx` into your app (e.g. `src/scenes/` or `components/three/`).

Install:

```bash
npm i three @react-three/fiber @react-three/drei
```

Do not extend this scene until it renders and orbits in the browser.
